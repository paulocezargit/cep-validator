# cep-validator
A simple utility that validates Brazilian ZIP codes (CEPs), checking whether the format follows the official standard. Ideal for preventing typing errors and ensuring valid addresses in forms and systems.
